# API Configuration
# Base URL only (e.g., https://api.example.com)
# The app will append the endpoints automatically:
# - POST /vox/upload/post (upload new post)
# - GET /vox/posts (fetch all posts)
VITE_API_URL=https://your-api-endpoint.com
